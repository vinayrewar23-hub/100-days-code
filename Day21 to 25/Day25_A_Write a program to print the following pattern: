#include <stdio.h>

int main()
{
    int i, j, n = 5;

    for(i = n; i >= 1; i--)   // outer loop controls rows
    {
        for(j = i; j <= n; j++)  // inner loop prints numbers
        {
            printf("%d", j);
        }
        printf("\n");
    }

    return 0;
}
