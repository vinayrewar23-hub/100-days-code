#include <iostream>
using namespace std;

int main() {
    int n, k;
    cin >> n;

    int arr[n];
    for (int i = 0; i < n; i++)
        cin >> arr[i];

    cin >> k;

    for (int i = 0; i <= n - k; i++) {
        int maxi = arr[i];   // start with first element in window
        
        for (int j = i; j < i + k; j++) { 
            if (arr[j] > maxi)
                maxi = arr[j];
        }
        
        cout << maxi << " ";
    }

    return 0;
}
