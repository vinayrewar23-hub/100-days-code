#include <stdio.h>
#include <math.h>

int main() {
    int n;
    scanf("%d", &n);

    long long total = (long long)n * (n + 1) / 2;
    int x = sqrt(total);

    if (1LL * x * x == total)
        printf("%d", x);
    else
        printf("-1");

    return 0;
}
